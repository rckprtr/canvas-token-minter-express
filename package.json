{
  "name": "canvas-token-minter",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "npm run build:server && npm run build:client",
    "build:server": "cd server && npm install && npm run build",
    "build:client": "cd client && npm install"
  },
  "author": "",
  "license": "ISC"
}
